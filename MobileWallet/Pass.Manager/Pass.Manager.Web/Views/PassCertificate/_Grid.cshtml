@(Html.Grid<PassCertificateAppleViewModel>()
    .Name("grdPassSites")
    .CssClass("pm-grid")
    .Pageable()
    .DataSource(ds => ds.Read(@Url.Action("GridSearch")))
    .Columns(c =>
                {
                    c.Bound(p => p.PassCertificateId).Title("ID").Width("100px");
                    c.BoundLink(p => p.Name, Url.Action("Edit"), id => id.PassCertificateId);
                    c.Bound(p => p.TeamId);
                    c.Bound(p => p.PassTypeId);
                    c.Bound(p => p.Description);
                    c.Bound(p => p.ExpDate);
                    c.Bound(p => p.PassCertificateId).Title("Action")
                        .GridAction("Edit", Url.Action("Edit"))
                        .GridAjaxAction("Delete", Url.Action("Delete"));
                    //c.Bound().Title("Test").ClientTemplateId("myTemp");
                }))

<!--
<script id="myTemp" type="text/x-kendo-template">
    # var date = eval(ExpDate.replace(/\/Date\((\d+)\)\//gi, "new Date($1)")); #
    <a id="cert#=PassCertificateId#" href="\\#">#=kendo.toString(date, 'd')#</a>

</script>
-->
